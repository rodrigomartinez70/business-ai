# [SUSPENDIDO] Informe Financiero por correo (hotel) — lunes 08:00.
# Correo financiero único top-down (reemplaza al dashboard + panel separados).
# Reactivar descomentando la línea de abajo.
# 0 8 * * 1 /cron/dashboard_semanal.sh >> /reportes/cron.log 2>&1

# [REEMPLAZADO por el scheduler] Informe Financiero del RESTAURANTE por correo.
# Ahora se gestiona desde MBI Admin (public.report_schedules) — ver el tick de abajo.
# 0 9 * * 1 /cron/dashboard_restaurante.sh >> /reportes/cron.log 2>&1

# Scheduler de reportes (DB-driven) — lee public.report_schedules y envía lo vencido.
# Cada 15 min; la API (hora Chile) decide qué disparar y dedupe por last_run.
*/15 * * * * /cron/run_schedules.sh >> /reportes/cron.log 2>&1

# [SUSPENDIDO] Sync diario de Meta Ads (restaurante_toteat) → Postgres — todos los días 07:00.
# Suspendido el 8-jun-2026: Meta bloqueó el acceso del token (OAuthException code 200).
# Reactivar (con token nuevo cargado vía scripts/set_meta_token.py) descomentando la línea de abajo.
# 0 7 * * * /cron/sync_meta.sh >> /reportes/cron.log 2>&1

# Sync diario de Toteat (restaurante_toteat) → vertical restaurante — 07:30
30 7 * * * /cron/sync_toteat.sh >> /reportes/cron.log 2>&1

# Reporte de uso/auditoría del sistema (markdown local) — lunes a las 09:00
0 9 * * 1 /cron/reporte_semanal.sh >> /reportes/cron.log 2>&1
